description = "SpringDoc Bridge for Protocol Buffers"

dependencies {
    api(project(":jackson-module-protobuf"))

    optional("com.fasterxml.jackson.core:jackson-databind:${jacksonVersion}")
    optional("tools.jackson.core:jackson-databind:${jackson3Version}")
    optional("org.springframework.boot:spring-boot-starter-web:${springBootVersion}")
    optional("org.springdoc:springdoc-openapi-starter-common:${springDocsVersion}")

    // Test dependencies
    testImplementation("org.springframework.boot:spring-boot-starter-test:${springBootVersion}")
    testImplementation("org.springdoc:springdoc-openapi-starter-webmvc-ui:${springDocsVersion}")
}

apply from: "${rootDir}/gradle/protobuf.gradle"
apply from: "${rootDir}/gradle/deploy.gradle"
